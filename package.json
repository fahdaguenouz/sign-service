{
  "name": "sign-service",
  "version": "1.0.0",
  "description": "Blockchain-based document verification web application.",
  "main": "server/index.js",
  "scripts": {
    "deploy": "npx hardhat compile && npx hardhat run scripts/deploy.js --network localhost",
    "serve": "node server/index.js",
    "start": "npm run deploy && npm run serve"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "ethers": "^6.10.0",
    "hardhat": "^2.19.4"
  }
}
